- provide(:title, @user.name)
-# debugger
%div{class: "row" }
 %div{ class: "span4" }
  %aside{ class: "span4" }
   %section 
    %h1
     = @user.name
     = gravatar_for @user
  %div.span8
   - if @user.microposts.any?
    %h3 
     Microposts 
     ( 
     = @user.microposts.count
     )
    %ol.microposts
     = render @microposts
    = will_paginate @microposts
    




